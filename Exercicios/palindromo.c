#include <stdio.h>
#include <ctype.h>
#define TAM 20

int main(){
    char frase[TAM];
    fgets(frase, TAM, stdin);

    return 0;
}

char palindromo(char *frase){
    int tamReal = strlen(frase) - 1;
    for(int i = 0; i < 0; i++){
        if(isspace(frase[i])){
            
        }
    }

}